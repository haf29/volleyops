[build]
builder = "RAILPACK"
buildCommand = "npm run build"

[deploy]
startCommand = "npx serve dist --single --listen tcp://0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
